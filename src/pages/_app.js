import 'tailwindcss/tailwind.css';

alert("No sense alert")

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
  console.log("Random unnecessary log");
}

export default MyApp;
